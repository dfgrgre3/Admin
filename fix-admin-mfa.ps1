Write-Host "This repository no longer contains privileged MFA-reset scripts." -ForegroundColor Yellow
Write-Host "Use the approved internal admin workflow for account recovery or credential resets." -ForegroundColor Yellow
Write-Host "Do not embed database URLs, service-role secrets, or user identifiers in repository scripts." -ForegroundColor Green
exit 0
